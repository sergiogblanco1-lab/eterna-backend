class VideoEngine:
    def generate_video(self, order_id: str, photos: list[str], phrases: list[str], output_path: str) -> str:
        raise NotImplementedError("VideoEngine limpio preparado. Aquí conectamos el generador real.")
